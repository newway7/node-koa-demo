const config = {
  // 启动端口
  port: 1200,

  // 数据库配置
  database: {
    DATABASE: 'node',
    USERNAME: 'root',
    PASSWORD: 'newway999',
    PORT: '3000',
    HOST: '127.0.0.1'
  }
}

module.exports = config
